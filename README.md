Requirements
1. Initialize the library by importing the library
2. Run the initialize function and pass the db pointer from gorm
3. Register your models 
4. gets package details from the models registered as the default but user can pass custom
5. Set the default output file name for generated code defaults to genmodels.go
6. Update the models with code taken from the generated stub
